import{c as i,$ as n,Q as s,j as d,m as t,h as c,ax as a,n as l,o as h,S as r}from"./chunks/framework.t4rEqR7d.js";const F=JSON.parse('{"title":"Choose a sandbox backend","description":"","frontmatter":{},"headers":[],"relativePath":"pagentv4/backends.md","filePath":"pagentv4/backends.md"}'),p={name:"pagentv4/backends.md"};function k(g,e,u,m,y,E){const o=n("Mermaid");return s(),d("div",null,[e[1]||(e[1]=t(`<h1 id="choose-a-sandbox-backend" tabindex="-1">Choose a sandbox backend <a class="header-anchor" href="#choose-a-sandbox-backend" aria-label="Permalink to &quot;Choose a sandbox backend&quot;">​</a></h1><p>Language: <a href="/pagent/zh/pagentv4/backends.html">简体中文</a> | English</p><p>pagent gives the agent a <strong>companion computer</strong> (the sandbox). There are <strong>four user-facing backends</strong>. They are siblings, not flavors of each other:</p><table tabindex="0"><thead><tr><th>Config value</th><th>Desktop / VS Code label</th><th>One sentence</th></tr></thead><tbody><tr><td><code>local</code></td><td>本机</td><td>Isolated scratch workspace on this machine</td></tr><tr><td><code>inplace</code></td><td>直接编辑</td><td>Edit the bound project directory in place</td></tr><tr><td><code>docker</code> / <code>podman</code> / <code>container</code></td><td>容器</td><td>Commands run in a container; files stay in the thread workspace</td></tr><tr><td><code>ssh</code></td><td>SSH / 远程</td><td>Commands and files on a remote host</td></tr></tbody></table><p><code>inplace</code> is <strong>not</strong> “inplace-docker” or “inplace-ssh”. Docker, SSH, and local do not “run inplace”; only <code>backend = &quot;inplace&quot;</code> writes the project directly.</p><p>API details (Runner, <code>Sandbox.create</code>, errors) stay on <a href="./sandbox.html">Sandbox</a>. This page is the map: what each backend touches, when to pick it, and the mix-ups that come up in the UI.</p><div class="warning custom-block"><p class="custom-block-title">Most common mix-up: <code>local</code> plus “agent, ssh there yourself”</p><p>A <strong><code>local</code></strong> session where you tell the model “ssh to that box and run it” is <strong>not</strong> the <code>ssh</code> backend.</p><ul><li><code>run_command(&quot;ssh host …&quot;)</code> is just another subprocess in the local workspace</li><li><code>read_file</code> / <code>write_file</code> / <code>list_dir</code> still hit this machine’s <code>workspaces/main/</code></li><li>Approvals, timeouts, resume, and the file tree are all local; pagent never sees remote files</li></ul><p>If the work belongs on the remote host: new task → <strong>SSH / 远程</strong>, or <code>[sandbox] backend = &quot;ssh&quot;</code>. Every tool then uses one asyncssh connection. Do not ask the model to assemble <code>ssh</code> by hand. Same idea: do not use <code>docker run</code> inside <code>local</code> as a substitute for the container backend.</p></div><h2 id="mental-model-two-directories" tabindex="-1">Mental model: two directories <a class="header-anchor" href="#mental-model-two-directories" aria-label="Permalink to &quot;Mental model: two directories&quot;">​</a></h2><p>Every session has two roots. Mixing them up is the usual source of confusion.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Agent paths          /home/agent/...     ← what the model sees</span></span>
<span class="line"><span>                     ↕ mapped by backend</span></span>
<span class="line"><span>workdir              where run_command / read_file / write_file actually go</span></span>
<span class="line"><span></span></span>
<span class="line"><span>host_root            the bound project on your machine</span></span>
<span class="line"><span>                     list_host_files / copy_from_host / copy_to_host only</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Directory</th><th>Typical location</th><th>Who uses it</th></tr></thead><tbody><tr><td><strong>workdir</strong> (agent computer)</td><td><code>local</code> / <code>docker</code>: <code>~/.pagent/threads/&lt;id&gt;/workspaces/main/</code> · <code>inplace</code>: the project itself · <code>ssh</code>: remote <code>~/pagent</code> (or <code>[sandbox.ssh] workdir</code>)</td><td><code>run_command</code>, <code>read_file</code>, <code>write_file</code>, <code>str_replace</code>, <code>list_dir</code></td></tr><tr><td><strong>host_root</strong> (your project)</td><td><code>[project.local] path</code> or <code>--project</code> / Desktop “项目目录”; empty → process cwd</td><td><code>list_host_files</code>, <code>copy_from_host</code>, <code>copy_to_host</code> → <code>&lt;host_root&gt;/artifacts/</code></td></tr></tbody></table><p><code>inplace</code> <strong>collapses</strong> the two: workdir and host_root are the same folder. Host-bridge tools are omitted because there is nothing left to copy.</p>`,12)),(s(),c(r,null,{default:a(()=>[h(o,{id:"mermaid-150",class:"mermaid",graph:"flowchart%20LR%0A%20%20subgraph%20agent%20%5BAgent%5D%0A%20%20%20%20tools%5B%22file%20%2B%20command%20tools%22%5D%0A%20%20end%0A%20%20subgraph%20localMode%20%5Blocal%5D%0A%20%20%20%20tw%5B%22thread%20workspace%22%5D%0A%20%20%20%20proj1%5B%22project%20via%20host%20tools%22%5D%0A%20%20end%0A%20%20subgraph%20inplaceMode%20%5Binplace%5D%0A%20%20%20%20proj2%5B%22project%20is%20the%20workspace%22%5D%0A%20%20end%0A%20%20subgraph%20dockerMode%20%5Bdocker%5D%0A%20%20%20%20ctr%5B%22container%20shell%22%5D%0A%20%20%20%20tw2%5B%22thread%20workspace%20bind-mounted%22%5D%0A%20%20end%0A%20%20subgraph%20sshMode%20%5Bssh%5D%0A%20%20%20%20remote%5B%22remote%20workdir%22%5D%0A%20%20end%0A%20%20tools%20--%3E%20localMode%0A%20%20tools%20--%3E%20inplaceMode%0A%20%20tools%20--%3E%20dockerMode%0A%20%20tools%20--%3E%20sshMode%0A%20%20localMode%20---%20tw%0A%20%20localMode%20---%20proj1%0A%20%20inplaceMode%20---%20proj2%0A%20%20dockerMode%20---%20ctr%0A%20%20dockerMode%20---%20tw2%0A%20%20sshMode%20---%20remote%0A"})]),fallback:a(()=>[...e[0]||(e[0]=[l(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=t(`<h2 id="side-by-side" tabindex="-1">Side-by-side <a class="header-anchor" href="#side-by-side" aria-label="Permalink to &quot;Side-by-side&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th><code>local</code></th><th><code>inplace</code></th><th><code>docker</code> / <code>podman</code></th><th><code>ssh</code></th></tr></thead><tbody><tr><td>Where commands run</td><td>This machine, in the thread workspace</td><td>This machine, in the project</td><td>Inside the container</td><td>Remote host</td></tr><tr><td>Where file tools write</td><td>Thread workspace</td><td><strong>The project</strong></td><td>Thread workspace (bind-mounted into the container)</td><td>Remote workdir</td></tr><tr><td>Isolated from the repo?</td><td>Yes, until you copy</td><td><strong>No</strong></td><td>Yes, until you copy</td><td>Yes, relative to your laptop</td></tr><tr><td>Needs extra software</td><td>No</td><td>No</td><td>Docker or Podman + an image</td><td>SSH config + reachable host</td></tr><tr><td>Host-bridge tools</td><td>Yes</td><td><strong>Removed</strong></td><td>Yes</td><td>Yes</td></tr><tr><td>Desktop sandbox file tab</td><td>Shown</td><td>Hidden (use <strong>项目</strong>)</td><td>Shown</td><td>Shown</td></tr><tr><td>Frozen in <code>thread.toml</code></td><td><code>backend</code></td><td><code>backend</code> + <code>project.path</code></td><td><code>backend</code> + <code>image</code></td><td><code>backend</code> + SSH fields</td></tr><tr><td>Resume from another cwd</td><td>Same workspace</td><td><strong>Same project path</strong></td><td>Same workspace</td><td>Same remote workdir</td></tr></tbody></table><p><code>container</code> is not a fifth mode. It picks <code>docker</code> or <code>podman</code> from <code>PATH</code> (docker first). <code>docker</code> and <code>podman</code> are the same design with a different CLI.</p><h2 id="local-—-isolated-workspace-on-this-machine" tabindex="-1"><code>local</code> — isolated workspace on this machine <a class="header-anchor" href="#local-—-isolated-workspace-on-this-machine" aria-label="Permalink to &quot;\`local\` — isolated workspace on this machine&quot;">​</a></h2><p>Default. Closest to “give the agent a scratch disk.”</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>~/.pagent/threads/&lt;thread_id&gt;/</span></span>
<span class="line"><span>  thread.toml</span></span>
<span class="line"><span>  messages/</span></span>
<span class="line"><span>  workspaces/main/     ← agent /home/agent</span></span></code></pre></div><p>The bound project is <strong>read/copy only</strong> through host tools. Edits in the workspace do not change the repo until <code>copy_to_host</code> (into <code>artifacts/</code>) or you copy by hand.</p><p><strong>Use when:</strong> trying pagent, generating throwaway files, or keeping the repo untouched until you accept an artifact.</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">backend = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;local&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">project</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">local</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># path = &quot;/path/to/repo&quot;   # host_root; empty → cwd at start</span></span></code></pre></div><h2 id="inplace-—-edit-the-project-in-place" tabindex="-1"><code>inplace</code> — edit the project in place <a class="header-anchor" href="#inplace-—-edit-the-project-in-place" aria-label="Permalink to &quot;\`inplace\` — edit the project in place&quot;">​</a></h2><p>This is the coding-CLI mode (<code>pagent -C .</code>). The agent’s <code>/home/agent</code> <strong>is</strong> the project. <code>write_file</code> / <code>str_replace</code> / <code>run_command</code> hit the real tree immediately.</p><p>Required: a project directory (<code>--project</code>, <code>-C</code>, Desktop 项目目录, or <code>[project.local] path</code>). The path is frozen in <code>thread.toml</code>, so resume keeps editing the same folder even if you start pagent elsewhere.</p><p>Tools: <code>run_command</code>, <code>read_file</code>, <code>write_file</code>, <code>str_replace</code>, <code>list_dir</code>. <code>list_host_files</code> / <code>copy_from_host</code> / <code>copy_to_host</code> are stripped — the project already <em>is</em> the working directory.</p><p><strong>Use when:</strong> you want the agent to work like Cursor/Claude Code on a git checkout. Keep the tree in version control and review tool approvals (<code>permission.mode = &quot;prompt&quot;</code> by default).</p><p>Shortcut:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pagent</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -C</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                    # current directory</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pagent</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -C</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /path/to/project</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # another directory</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># same as: pagent --backend inplace --project &lt;dir&gt;</span></span></code></pre></div><p>Safe first run (throwaway folder, not your real repo):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/pagent-inplace-test</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;alpha&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/pagent-inplace-test/hello.txt</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pagent</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -C</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/pagent-inplace-test</span></span></code></pre></div><p>Ask it to replace <code>alpha</code> with <code>beta</code> in <code>hello.txt</code>, then:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/pagent-inplace-test/hello.txt</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # beta</span></span></code></pre></div><p>Conversation metadata still lives under <code>~/.pagent/threads/</code>. There is <strong>no</strong> separate <code>workspaces/main/</code> for this mode.</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">backend = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;inplace&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">project</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">local</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">path = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/path/to/project&quot;</span></span></code></pre></div><h2 id="docker-podman-container-—-commands-in-a-container" tabindex="-1"><code>docker</code> / <code>podman</code> / <code>container</code> — commands in a container <a class="header-anchor" href="#docker-podman-container-—-commands-in-a-container" aria-label="Permalink to &quot;\`docker\` / \`podman\` / \`container\` — commands in a container&quot;">​</a></h2><p>Commands run <strong>inside</strong> the image. The thread workspace is bind-mounted at the <strong>same host path</strong> inside the container (<code>-v &lt;workdir&gt;:&lt;workdir&gt;</code>). File tools therefore still write on the host workspace, not into the image layer, and not into your project unless you use host-bridge tools.</p><p>This is <strong>not</strong> inplace: the repo is not the container workdir.</p><p>Needs: CLI on <code>PATH</code> and a local image (<code>[sandbox.container] image</code>). Missing images are not pulled automatically; <code>docker run</code> fails and the UI surfaces it.</p><p><strong>Use when:</strong> you want a known Linux userland (compilers, Python, headless browser) without installing them on the Mac, while keeping files on disk.</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">backend = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;container&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # or docker / podman</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># tools = [...]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">container</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">image = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;pagent:latest&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">container_ttl = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">300</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       # seconds; 0 / unset → sleep infinity</span></span></code></pre></div><p>Browser / screenshot image:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pagent:browser</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src/app/Dockerfile.browser</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src/app</span></span></code></pre></div><p>Then set <code>image = &quot;pagent:browser&quot;</code>.</p><p>Python API:</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">runner </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Runner.create(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;demo&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    provider,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    overrides</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;backend&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;docker&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;image&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;python:3.12-slim&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">},</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><h2 id="ssh-—-remote-host" tabindex="-1"><code>ssh</code> — remote host <a class="header-anchor" href="#ssh-—-remote-host" aria-label="Permalink to &quot;\`ssh\` — remote host&quot;">​</a></h2><p><code>run_command</code> and file tools use one long-lived <code>asyncssh</code> connection. The agent’s <code>/home/agent</code> maps to the <strong>remote</strong> workdir (<code>[sandbox.ssh] workdir</code>, default <code>~/pagent</code>, created if missing). Host-bridge tools still talk to the <strong>local</strong> <code>host_root</code>.</p><p>Needs: <code>host</code> as a <code>Host</code> alias in <code>~/.ssh/config</code> (User / Hostname / IdentityFile as usual). Connect uses <code>connect_timeout=10s</code> and <code>login_timeout=15s</code> so a dead host fails instead of hanging the UI.</p><p><strong>Use when:</strong> the job belongs on a GPU box, HPC, or a machine that already has the toolchain. Do not stay on <code>local</code> and ask the model to <code>ssh</code>; that only starts a local client, and file tools still write locally.</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">backend = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ssh&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config_path = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/.ssh/config&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">host = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;machine_root&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # Host alias, not user@hostname</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">workdir = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/pagent&quot;</span></span></code></pre></div><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">runner </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Runner.create(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;remote&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    provider,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    overrides</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;backend&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ssh&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;ssh_host&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;machine_root&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;ssh_workdir&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/pagent&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><h2 id="how-to-choose" tabindex="-1">How to choose <a class="header-anchor" href="#how-to-choose" aria-label="Permalink to &quot;How to choose&quot;">​</a></h2><ol><li><strong>Just chat / generate files I will copy later</strong> → <code>local</code></li><li><strong>Refactor this git repo on my laptop</strong> → <code>inplace</code> (<code>pagent -C .</code>)</li><li><strong>Need Ubuntu packages / a pinned image, keep files on this machine</strong> → <code>container</code> / <code>docker</code></li><li><strong>The work must happen on another computer</strong> → the <strong><code>ssh</code> backend</strong> (do not stay on <code>local</code> and tell the model to ssh)</li></ol><p>Decision shortcuts:</p><ul><li>“Will <code>write_file(&quot;app.py&quot;)</code> change my repo?” Only <strong><code>inplace</code></strong>.</li><li>“Do I need Docker?” Only <strong>container family</strong>.</li><li>“Is the project mounted into the sandbox?” <strong><code>inplace</code></strong>: the project <em>is</em> the sandbox. <strong><code>local</code> / <code>docker</code></strong>: the project is host_root; copy in/out.</li><li>“Can I resume on another laptop?” <code>local</code>/<code>docker</code> workspaces stay on the original machine’s disk. <code>inplace</code> is that machine’s project path. <code>ssh</code> is whatever remote you configured.</li></ul><h2 id="config-and-freeze" tabindex="-1">Config and freeze <a class="header-anchor" href="#config-and-freeze" aria-label="Permalink to &quot;Config and freeze&quot;">​</a></h2><p>Global <code>pagent.toml</code> is the default for <strong>new</strong> threads. Each thread freezes backend + project + image/SSH into its <code>thread.toml</code>; later <code>pagent.toml</code> edits do not rewrite an existing conversation.</p><p>Desktop <strong>新建任务</strong> can override per session. Labels:</p><table tabindex="0"><thead><tr><th>UI</th><th><code>backend=</code></th></tr></thead><tbody><tr><td>本机</td><td><code>local</code></td></tr><tr><td>直接编辑</td><td><code>inplace</code></td></tr><tr><td>容器</td><td><code>container</code> (or <code>docker</code> / <code>podman</code>)</td></tr><tr><td>SSH / 远程</td><td><code>ssh</code></td></tr></tbody></table><p>Onboarding “沙箱” picks a <strong>preferred</strong> default; you still choose per task.</p><h2 id="common-mix-ups" tabindex="-1">Common mix-ups <a class="header-anchor" href="#common-mix-ups" aria-label="Permalink to &quot;Common mix-ups&quot;">​</a></h2><p><strong><code>local</code> plus the agent running <code>ssh</code> / <code>docker run</code></strong><br> The backend is where <strong>every</strong> tool lands, not where the chat says the work should happen. <code>local</code> + <code>run_command(&quot;ssh gpu01 make&quot;)</code>:</p><table tabindex="0"><thead><tr><th></th><th>DIY <code>ssh</code> on <code>local</code></th><th><code>backend = &quot;ssh&quot;</code></th></tr></thead><tbody><tr><td><code>run_command</code></td><td>local ssh client</td><td>remote shell</td></tr><tr><td><code>write_file(&quot;a.py&quot;)</code></td><td>local workspace</td><td>remote workdir</td></tr><tr><td>Hang / timeout</td><td>child process can stall the UI</td><td>connect timeout, error in UI</td></tr><tr><td>File tree</td><td>local <code>workspaces/main/</code></td><td>remote directory</td></tr><tr><td>Keys / Host</td><td>the model guesses CLI flags</td><td>your <code>~/.ssh/config</code></td></tr></tbody></table><p>Need the remote machine → switch backend. <code>ssh</code> is not a way to use <code>local</code>.</p><p><strong>“inplace docker / inplace ssh / inplace local”</strong><br> There is no such combo. One <code>backend</code> per thread. Subagents may set <code>[sub.&lt;name&gt;] backend</code> (or <code>&quot;none&quot;</code>) independently; they do not create a hybrid inplace-docker.</p><p><strong><code>local</code> vs <code>inplace</code></strong><br> Both run on this machine. <code>local</code> = scratch workspace. <code>inplace</code> = the repo.</p><p><strong><code>docker</code> vs <code>inplace</code></strong><br> Docker isolates the <em>shell</em>, not your project. Files land in <code>workspaces/main/</code> unless you copy.</p><p><strong><code>[project.local] path</code> on <code>local</code>/<code>docker</code>/<code>ssh</code></strong><br> That path is host_root (observe / copy), not the command cwd. Only <code>inplace</code> uses it as workdir.</p><p><strong><code>container</code> vs <code>docker</code> vs <code>podman</code></strong><br> Same backend class. <code>container</code> auto-detects. Pin <code>docker</code> or <code>podman</code> if both CLIs exist and you care which one runs.</p><p><strong>Empty <code>inplace</code> project</strong><br><code>inplace</code> without a directory is a config error (<code>project_path</code> required).</p><p><strong>SSH hang</strong><br> Unreachable hosts used to block the wire loop. Connect now times out; status polling does not open the sandbox. Check <code>Host</code> alias, keys, and network.</p><p><strong><code>command_policy = &quot;workdir&quot;</code></strong><br> Applies to all backends: <code>run_command</code> cannot pass paths outside the sandbox workdir. <code>open</code> disables that check.</p><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="./sandbox.html">Sandbox API</a> — <code>Runner.create</code>, tools, limits, errors</li><li><a href="./tools.html">Tools</a> — the eight sandbox tools</li><li><a href="/pagent/desktop.html">Desktop</a> — 本机 / 直接编辑 / 容器 / 远程 in the app</li><li><a href="/pagent/vscode.html">VS Code</a> — <code>pagent.toml</code> <code>[sandbox]</code> block</li><li>Template: <code>src/template/pagent.toml</code></li></ul>`,62))])}const C=i(p,[["render",k]]);export{F as __pageData,C as default};
