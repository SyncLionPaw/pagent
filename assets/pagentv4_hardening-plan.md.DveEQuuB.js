import{c as o,Q as n,j as d,m as t}from"./chunks/framework.t4rEqR7d.js";const u=JSON.parse('{"title":"pagentv4 runtime 夯实计划","description":"","frontmatter":{},"headers":[],"relativePath":"pagentv4/hardening-plan.md","filePath":"pagentv4/hardening-plan.md"}'),r={name:"pagentv4/hardening-plan.md"};function c(a,e,s,p,i,l){return n(),d("div",null,[...e[0]||(e[0]=[t('<h1 id="pagentv4-runtime-夯实计划" tabindex="-1">pagentv4 runtime 夯实计划 <a class="header-anchor" href="#pagentv4-runtime-夯实计划" aria-label="Permalink to &quot;pagentv4 runtime 夯实计划&quot;">​</a></h1><p><strong>范围</strong>:<code>src/pagentv4/runtime/</code> 与 <code>src/pagentv4/sandbox/</code>。 <strong>目标</strong>:巩固 runner 继承链重构,消除遗留重复与约定漂移,不引入新功能。</p><p><strong>验收总则</strong>:每项完成需满足 <code>ruff check</code> + <code>ruff format --check</code> 干净、<code>pytest tests/</code> 全绿、涉及英文文档改动时同步 <code>llms-full.txt</code>。</p><hr><h2 id="已完成" tabindex="-1">已完成 <a class="header-anchor" href="#已完成" aria-label="Permalink to &quot;已完成&quot;">​</a></h2><p>以下五项已落地并随代码提交,详情见对应 commit 与源码:</p><ul><li><strong>P0-1 三处资源初始化逻辑重复</strong>:抽出 <code>assemble_run_resources</code> + <code>RunResources</code>(<a href="./../../src/pagentv4/runtime/base_runner.py.html">base_runner.py</a>),三处统一调用,删除 <code>open_code_resources</code>。</li><li><strong>P1-1 CodeRunner 初始化时序统一</strong>:配置入口收敛到 <code>__init__</code>,<code>create</code> / <code>from_toml</code> 复用同一路径;懒 / 立即两条时序契约写进模块 docstring(<a href="./../../src/pagentv4/runtime/code_runner.py.html">code_runner.py</a>)。</li><li><strong>P1-2 Thread.open_sandbox 与 backend 映射解耦</strong>:backend 映射下沉到 <code>open_sandbox_for_spec</code> 工厂(<a href="./../../src/pagentv4/sandbox/sandbox.py.html">sandbox.py</a>),<code>Thread.open_sandbox</code> 简化为委托调用。</li><li><strong>P2-1 ThreadSpec 字段分组</strong>:用 <code>toml_field(section, key, default)</code> 把 <code>[section] key</code> 绑定进 dataclass metadata,<code>to_dict</code> / <code>from_dict</code> 从 <code>section_bindings()</code> 推导(<a href="./../../src/pagentv4/ithread/__init__.py.html">ithread/<strong>init</strong>.py</a>)。</li><li><strong>P2-2 backend 错误处理口径统一</strong>:新增 <code>SandboxError</code> / <code>SandboxNotStartedError</code>(<a href="./../../src/pagentv4/sandbox/base.py.html">base.py</a>),四类边界写进 <code>Backend</code> Protocol docstring,<code>docs/pagentv4/sandbox.md</code> 补错误处理表。</li></ul><p>测试基线:<code>pytest tests/</code> 473 passed / 5 skipped。</p><hr><h2 id="待办" tabindex="-1">待办 <a class="header-anchor" href="#待办" aria-label="Permalink to &quot;待办&quot;">​</a></h2><h3 id="p0-2-inbound-与-tool-hooks-按新架构定型" tabindex="-1">P0-2 inbound 与 tool hooks 按新架构定型 <a class="header-anchor" href="#p0-2-inbound-与-tool-hooks-按新架构定型" aria-label="Permalink to &quot;P0-2 inbound 与 tool hooks 按新架构定型&quot;">​</a></h3><p><strong>现状</strong><code>Runner</code> 已继承 <code>BaseRunner</code>,但 inbound 检查点(<code>emit</code> 内 <code>_apply_inbound_drain</code>)、<code>RunCancelled</code> 捕获(<code>_event_source</code>)、hooks 调度(<code>run_tool_with_hooks</code>)三段逻辑沿用重构前实现,作为后续重构的参考保留(见 <a href="./../../src/pagentv4/runtime/runner.py.html">runner.py</a> L126-229)。checkpoint / drain 机制与 <code>emit</code> 骨架的边界尚未定型。REPL 实际使用带 inbound + hooks 的 <code>Runner.create()</code>(见 <a href="./../../src/app/repl.py.html">repl.py</a>),属当前生产路径。</p><p><strong>目标</strong> 明确 inbound 与 hooks 作为「可叠加能力」挂进骨架的方式:界定 <code>emit</code> 检查点契约、<code>RunCancelled</code> 的传播路径、hooks 的前后置插入点,让 <code>Runner</code> 只覆写真差异点而不重写循环。保持公开 API(<code>runner.steer</code> / <code>cancel_run</code> / <code>permit_tool</code> / <code>deny_tool</code>、<code>Runner.create</code>)不变。</p><p><strong>验收</strong><code>test_pagentv4_runner_inbound</code> / <code>test_pagentv4_inbound</code> / <code>test_pagentv4_tool_hooks</code> 全绿;REPL(<code>app/repl.py</code>、<code>app/tool_permit.py</code>)行为不变。</p><p><strong>说明</strong>:独立且影响 REPL,单独一个迭代处理,配合回归测试。</p>',15)])])}const h=o(r,[["render",c]]);export{u as __pageData,h as default};
