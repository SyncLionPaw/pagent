import{c as o,$ as d,Q as a,j as n,m as t,h as p,ax as e,n as c,o as l,S as h}from"./chunks/framework.t4rEqR7d.js";const m=JSON.parse('{"title":"怎么选沙箱后端","description":"","frontmatter":{},"headers":[],"relativePath":"zh/pagentv4/backends.md","filePath":"zh/pagentv4/backends.md"}'),r={name:"zh/pagentv4/backends.md"};function k(g,s,E,u,b,y){const i=d("Mermaid");return a(),n("div",null,[s[1]||(s[1]=t(`<h1 id="怎么选沙箱后端" tabindex="-1">怎么选沙箱后端 <a class="header-anchor" href="#怎么选沙箱后端" aria-label="Permalink to &quot;怎么选沙箱后端&quot;">​</a></h1><p>语言：<a href="/pagent/zh/pagentv4/backends.html">中文</a> | <a href="/pagent/pagentv4/backends.html">English</a></p><p>pagent 给 agent 配一台<strong>伴身电脑</strong>（sandbox）。面向用户的后端是 <strong>四种并列选项</strong>， 不是「inplace 的四种写法」：</p><table tabindex="0"><thead><tr><th>配置值</th><th>桌面 / 插件上的名字</th><th>一句话</th></tr></thead><tbody><tr><td><code>local</code></td><td>本机</td><td>本机上的独立 scratch workspace</td></tr><tr><td><code>inplace</code></td><td>直接编辑</td><td>直接改绑定的项目目录</td></tr><tr><td><code>docker</code> / <code>podman</code> / <code>container</code></td><td>容器</td><td>命令在容器里跑，文件仍在 thread workspace</td></tr><tr><td><code>ssh</code></td><td>SSH / 远程</td><td>命令和文件都在远端主机上</td></tr></tbody></table><p><strong><code>inplace</code> 不是 inplace-docker、也不是 inplace-ssh。</strong> Docker、SSH、本机都不会 「按 inplace 方式改仓库」；只有 <code>backend = &quot;inplace&quot;</code> 才会原地写项目。</p><p>Runner / <code>Sandbox.create</code> / 错误类型见 <a href="./sandbox.html">Sandbox</a>。本页只讲：每种后端 动哪块磁盘、什么时候选、界面上容易混的地方。</p><div class="warning custom-block"><p class="custom-block-title">最常见的误用：本机模式里让 agent 自己 ssh</p><p>会话开成 <strong><code>local</code>（本机）</strong>，再在对话里说「ssh 到那台机器上跑」——这<strong>不是</strong><code>ssh</code> 后端。</p><ul><li><code>run_command(&quot;ssh host …&quot;)</code> 只是本机 workspace 里又开了一条 ssh 子进程</li><li><code>read_file</code> / <code>write_file</code> / <code>list_dir</code> <strong>仍然打在本机</strong> <code>workspaces/main/</code></li><li>审批、超时、resume、文件树看到的都是本机沙箱，远端文件 pagent 看不见</li></ul><p>活要发生在远端时：新建任务选 <strong>SSH / 远程</strong>，或 <code>[sandbox] backend = &quot;ssh&quot;</code>。 让全部工具走同一条 asyncssh 连接，而不是让模型自己拼 <code>ssh</code> 命令。 同理：不要在 <code>local</code> 里让 agent <code>docker run</code> 来「当容器后端」。</p></div><h2 id="先记住两个目录" tabindex="-1">先记住两个目录 <a class="header-anchor" href="#先记住两个目录" aria-label="Permalink to &quot;先记住两个目录&quot;">​</a></h2><p>每个会话都有两棵根。混在一起就会觉得「四种 inplace」。</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Agent 看到的路径     /home/agent/...     ← 模型眼里的家</span></span>
<span class="line"><span>                     ↕ 由后端映射</span></span>
<span class="line"><span>workdir              run_command / read_file / write_file 真正落点</span></span>
<span class="line"><span></span></span>
<span class="line"><span>host_root            你本机绑定的项目</span></span>
<span class="line"><span>                     仅 list_host_files / copy_from_host / copy_to_host</span></span></code></pre></div><table tabindex="0"><thead><tr><th>目录</th><th>常见位置</th><th>谁用</th></tr></thead><tbody><tr><td><strong>workdir</strong>（agent 的电脑）</td><td><code>local</code> / <code>docker</code>：<code>~/.pagent/threads/&lt;id&gt;/workspaces/main/</code> · <code>inplace</code>：项目本身 · <code>ssh</code>：远端 <code>~/pagent</code>（或 <code>[sandbox.ssh] workdir</code>）</td><td><code>run_command</code>、<code>read_file</code>、<code>write_file</code>、<code>str_replace</code>、<code>list_dir</code></td></tr><tr><td><strong>host_root</strong>（你的项目）</td><td><code>[project.local] path</code> 或 <code>--project</code> / 桌面「项目目录」；留空 → 启动时的 cwd</td><td><code>list_host_files</code>、<code>copy_from_host</code>、<code>copy_to_host</code> → <code>&lt;host_root&gt;/artifacts/</code></td></tr></tbody></table><p><code>inplace</code> 把两棵根<strong>收成同一文件夹</strong>。宿主机桥接工具会被拿掉：已经没有「再拷一份」 的必要。</p>`,12)),(a(),p(h,null,{default:e(()=>[l(i,{id:"mermaid-150",class:"mermaid",graph:"flowchart%20LR%0A%20%20subgraph%20agent%20%5BAgent%5D%0A%20%20%20%20tools%5B%22%E6%96%87%E4%BB%B6%20%2B%20%E5%91%BD%E4%BB%A4%E5%B7%A5%E5%85%B7%22%5D%0A%20%20end%0A%20%20subgraph%20localMode%20%5Blocal%20%E6%9C%AC%E6%9C%BA%5D%0A%20%20%20%20tw%5B%22thread%20workspace%22%5D%0A%20%20%20%20proj1%5B%22%E9%A1%B9%E7%9B%AE%EF%BC%9A%E5%8F%AA%E8%83%BD%E8%A7%82%E5%AF%9F%2F%E6%8B%B7%E8%B4%9D%22%5D%0A%20%20end%0A%20%20subgraph%20inplaceMode%20%5Binplace%20%E7%9B%B4%E6%8E%A5%E7%BC%96%E8%BE%91%5D%0A%20%20%20%20proj2%5B%22%E9%A1%B9%E7%9B%AE%20%3D%20workspace%22%5D%0A%20%20end%0A%20%20subgraph%20dockerMode%20%5Bdocker%20%E5%AE%B9%E5%99%A8%5D%0A%20%20%20%20ctr%5B%22%E5%AE%B9%E5%99%A8%E9%87%8C%E7%9A%84%20shell%22%5D%0A%20%20%20%20tw2%5B%22thread%20workspace%20bind%20mount%22%5D%0A%20%20end%0A%20%20subgraph%20sshMode%20%5Bssh%20%E8%BF%9C%E7%A8%8B%5D%0A%20%20%20%20remote%5B%22%E8%BF%9C%E7%AB%AF%20workdir%22%5D%0A%20%20end%0A%20%20tools%20--%3E%20localMode%0A%20%20tools%20--%3E%20inplaceMode%0A%20%20tools%20--%3E%20dockerMode%0A%20%20tools%20--%3E%20sshMode%0A"})]),fallback:e(()=>[...s[0]||(s[0]=[c(" Loading... ",-1)])]),_:1})),s[2]||(s[2]=t(`<h2 id="对照表" tabindex="-1">对照表 <a class="header-anchor" href="#对照表" aria-label="Permalink to &quot;对照表&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th><code>local</code></th><th><code>inplace</code></th><th><code>docker</code> / <code>podman</code></th><th><code>ssh</code></th></tr></thead><tbody><tr><td>命令跑在哪</td><td>本机，thread workspace</td><td>本机，项目目录</td><td>容器内</td><td>远端主机</td></tr><tr><td>文件工具写到哪</td><td>thread workspace</td><td><strong>项目目录</strong></td><td>thread workspace（bind 进容器同名路径）</td><td>远端 workdir</td></tr><tr><td>和仓库隔离吗</td><td>是，直到你拷出去</td><td><strong>否</strong></td><td>是，直到你拷出去</td><td>相对这台笔记本是隔离的</td></tr><tr><td>额外依赖</td><td>无</td><td>无</td><td>Docker 或 Podman + 镜像</td><td>SSH config + 能连上的机器</td></tr><tr><td>宿主机桥接工具</td><td>有</td><td><strong>不挂</strong></td><td>有</td><td>有</td></tr><tr><td>桌面「沙箱」文件 Tab</td><td>显示</td><td>隐藏（看 <strong>项目</strong>）</td><td>显示</td><td>显示</td></tr><tr><td>冻进 <code>thread.toml</code></td><td><code>backend</code></td><td><code>backend</code> + <code>project.path</code></td><td><code>backend</code> + <code>image</code></td><td><code>backend</code> + SSH 字段</td></tr><tr><td>换个 cwd 再 resume</td><td>还是那个 workspace</td><td><strong>还是那个项目路径</strong></td><td>还是那个 workspace</td><td>还是那个远端目录</td></tr></tbody></table><p><code>container</code> 不是第五种。它从 <code>PATH</code> 里探测 <code>docker</code> 或 <code>podman</code>（优先 docker）。 <code>docker</code> 和 <code>podman</code> 是同一套设计，只是 CLI 不同。</p><h2 id="local-—-本机独立-workspace" tabindex="-1"><code>local</code> — 本机独立 workspace <a class="header-anchor" href="#local-—-本机独立-workspace" aria-label="Permalink to &quot;\`local\` — 本机独立 workspace&quot;">​</a></h2><p>默认。最接近「给 agent 一块草稿纸」。</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>~/.pagent/threads/&lt;thread_id&gt;/</span></span>
<span class="line"><span>  thread.toml</span></span>
<span class="line"><span>  messages/</span></span>
<span class="line"><span>  workspaces/main/     ← agent 的 /home/agent</span></span></code></pre></div><p>绑定的项目只能通过宿主机工具<strong>看 / 拷</strong>。workspace 里的修改不会进仓库，除非 <code>copy_to_host</code>（写到 <code>artifacts/</code>）或你手动拷。</p><p><strong>适合：</strong> 先试用 pagent、生成一次性文件、或仓库先别动、只收产物。</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">backend = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;local&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">project</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">local</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># path = &quot;/path/to/repo&quot;   # host_root；留空 = 启动时的 cwd</span></span></code></pre></div><h2 id="inplace-—-直接改项目" tabindex="-1"><code>inplace</code> — 直接改项目 <a class="header-anchor" href="#inplace-—-直接改项目" aria-label="Permalink to &quot;\`inplace\` — 直接改项目&quot;">​</a></h2><p>这就是 coding CLI 模式（<code>pagent -C .</code>）。agent 的 <code>/home/agent</code> <strong>就是</strong>项目。 <code>write_file</code> / <code>str_replace</code> / <code>run_command</code> 立刻打到真实目录。</p><p>必须有项目目录（<code>--project</code>、<code>-C</code>、桌面「项目目录」、或 <code>[project.local] path</code>）。 路径冻进 <code>thread.toml</code>，之后从别的目录 resume，仍改原来那个文件夹。</p><p>工具只有：<code>run_command</code>、<code>read_file</code>、<code>write_file</code>、<code>str_replace</code>、<code>list_dir</code>。 不挂 <code>list_host_files</code> / <code>copy_from_host</code> / <code>copy_to_host</code>——项目已经是工作目录。</p><p><strong>适合：</strong> 想像 Cursor / Claude Code 那样改一份 git 仓库。务必用版本控制，并保留 工具审批（默认 <code>permission.mode = &quot;prompt&quot;</code>）。</p><p>快捷写法：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pagent</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -C</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                    # 当前目录</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pagent</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -C</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /path/to/project</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # 别的目录</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 等价于：pagent --backend inplace --project &lt;dir&gt;</span></span></code></pre></div><p>第一次建议用临时目录，别拿真正的仓库试：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/pagent-inplace-test</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;alpha&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/pagent-inplace-test/hello.txt</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pagent</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -C</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/pagent-inplace-test</span></span></code></pre></div><p>让它把 <code>hello.txt</code> 里的 <code>alpha</code> 改成 <code>beta</code>，然后：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/pagent-inplace-test/hello.txt</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # beta</span></span></code></pre></div><p>对话和配置仍在 <code>~/.pagent/threads/</code>。这种模式<strong>不会</strong>再开一份 <code>workspaces/main/</code>。</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">backend = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;inplace&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">project</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">local</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">path = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/path/to/project&quot;</span></span></code></pre></div><h2 id="docker-podman-container-—-命令进容器" tabindex="-1"><code>docker</code> / <code>podman</code> / <code>container</code> — 命令进容器 <a class="header-anchor" href="#docker-podman-container-—-命令进容器" aria-label="Permalink to &quot;\`docker\` / \`podman\` / \`container\` — 命令进容器&quot;">​</a></h2><p>命令在镜像里执行。thread workspace 以<strong>宿主机同名路径</strong> bind mount 进容器 （<code>-v &lt;workdir&gt;:&lt;workdir&gt;</code>）。所以文件工具写的是本机 workspace，不是镜像层， 也不是你的项目（除非再用宿主机桥接工具拷）。</p><p>这<strong>不是</strong> inplace：仓库不会变成容器工作目录。</p><p>需要：<code>PATH</code> 里有 CLI，以及本地镜像（<code>[sandbox.container] image</code>）。缺镜像不会 自动 pull，<code>docker run</code> 失败后错误回到界面。</p><p><strong>适合：</strong> 要固定的 Linux 用户态（编译器、Python、无头浏览器），又不想装到 Mac 上， 文件仍留在本机磁盘。</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">backend = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;container&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 或 docker / podman</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">container</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">image = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;pagent:latest&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">container_ttl = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">300</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       # 秒；0 / 不设 = sleep infinity</span></span></code></pre></div><p>截图 / 渲染 HTML / PDF 用 browser 镜像：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pagent:browser</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src/app/Dockerfile.browser</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src/app</span></span></code></pre></div><p>然后把 <code>image</code> 改成 <code>pagent:browser</code>。</p><p>Python：</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">runner </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Runner.create(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;demo&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    provider,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    overrides</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;backend&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;docker&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;image&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;python:3.12-slim&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">},</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><h2 id="ssh-—-远端主机" tabindex="-1"><code>ssh</code> — 远端主机 <a class="header-anchor" href="#ssh-—-远端主机" aria-label="Permalink to &quot;\`ssh\` — 远端主机&quot;">​</a></h2><p><code>run_command</code> 和文件工具共用一条长连接（asyncssh）。agent 的 <code>/home/agent</code> 映射到 <strong>远端</strong> workdir（<code>[sandbox.ssh] workdir</code>，默认 <code>~/pagent</code>，不存在会 mkdir）。 宿主机桥接工具仍然打到<strong>本机</strong> <code>host_root</code>。</p><p>需要：<code>~/.ssh/config</code> 里的 <code>Host</code> 别名（User / Hostname / IdentityFile 和平时 <code>ssh &lt;alias&gt;</code> 一样）。连接带 <code>connect_timeout=10s</code>、<code>login_timeout=15s</code>，连不上 会尽快失败，而不是把界面卡死。</p><p><strong>适合：</strong> 活必须跑在 GPU 机、超算、或已经装好工具链的那台机器上。 不要开 <code>local</code> 再让模型 <code>ssh</code>；那只会在本机再起一个客户端，文件工具仍写本机。</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">backend = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ssh&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config_path = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/.ssh/config&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">host = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;machine_root&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # Host 别名，不是 user@hostname</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">workdir = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/pagent&quot;</span></span></code></pre></div><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">runner </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Runner.create(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;remote&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    provider,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    overrides</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;backend&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ssh&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;ssh_host&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;machine_root&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;ssh_workdir&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/pagent&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><h2 id="怎么选" tabindex="-1">怎么选 <a class="header-anchor" href="#怎么选" aria-label="Permalink to &quot;怎么选&quot;">​</a></h2><ol><li><strong>先聊着、文件以后再拷</strong> → <code>local</code></li><li><strong>就在这份 git 仓库上改代码</strong> → <code>inplace</code>（<code>pagent -C .</code>）</li><li><strong>要 Ubuntu 包 / 固定镜像，文件留在这台电脑</strong> → <code>container</code> / <code>docker</code></li><li><strong>活必须发生在另一台机器</strong> → <strong><code>ssh</code> 后端</strong>（不要开 <code>local</code> 再让模型自己 ssh）</li></ol><p>快速判断：</p><ul><li>「<code>write_file(&quot;app.py&quot;)</code> 会改我的仓库吗？」只有 <strong><code>inplace</code></strong> 会。</li><li>「要不要装 Docker？」只有 <strong>容器这一家</strong> 要。</li><li>「项目有没有挂进沙箱？」<strong><code>inplace</code></strong>：项目就是沙箱。<strong><code>local</code> / <code>docker</code></strong>： 项目是 host_root，靠拷进拷出。</li><li>「换台笔记本 resume？」<code>local</code>/<code>docker</code> 的 workspace 在原机磁盘上；<code>inplace</code> 绑的是原机那条项目路径；<code>ssh</code> 跟你配的远端走。</li></ul><h2 id="配置和冻结" tabindex="-1">配置和冻结 <a class="header-anchor" href="#配置和冻结" aria-label="Permalink to &quot;配置和冻结&quot;">​</a></h2><p>全局 <code>pagent.toml</code> 只管<strong>新建</strong>对话的缺省。每个 thread 会把 backend、项目、 镜像 / SSH 冻进自己的 <code>thread.toml</code>；之后改全局文件，不会改写已有会话。</p><p>桌面 <strong>新建任务</strong> 可以按会话覆盖。界面文案对应：</p><table tabindex="0"><thead><tr><th>界面</th><th><code>backend=</code></th></tr></thead><tbody><tr><td>本机</td><td><code>local</code></td></tr><tr><td>直接编辑</td><td><code>inplace</code></td></tr><tr><td>容器</td><td><code>container</code>（或 <code>docker</code> / <code>podman</code>）</td></tr><tr><td>SSH / 远程</td><td><code>ssh</code></td></tr></tbody></table><p>首次设置向导里的「沙箱」只是<strong>偏好默认值</strong>，每个任务仍可另选。</p><h2 id="常见误解" tabindex="-1">常见误解 <a class="header-anchor" href="#常见误解" aria-label="Permalink to &quot;常见误解&quot;">​</a></h2><p><strong><code>local</code> 里让 agent 自己 <code>ssh</code> / <code>docker run</code></strong><br> 后端决定的是<strong>每一件工具</strong>落在哪，不是对话里口头说的「去哪干活」。 <code>local</code> + <code>run_command(&quot;ssh gpu01 make&quot;)</code>：</p><table tabindex="0"><thead><tr><th></th><th><code>local</code> 里自己 ssh</th><th><code>backend = &quot;ssh&quot;</code></th></tr></thead><tbody><tr><td><code>run_command</code></td><td>本机起 ssh 客户端</td><td>远端 shell</td></tr><tr><td><code>write_file(&quot;a.py&quot;)</code></td><td>本机 workspace</td><td>远端 workdir</td></tr><tr><td>连不上 / 超时</td><td>子进程挂起，界面像卡住</td><td>连接超时，错误回到 UI</td></tr><tr><td>桌面文件树</td><td>本机 <code>workspaces/main/</code></td><td>远端目录</td></tr><tr><td>密钥 / Host</td><td>agent 自己猜命令行</td><td>读你的 <code>~/.ssh/config</code></td></tr></tbody></table><p>要远端就换后端，不要把 ssh 当成 <code>local</code> 的一种用法。</p><p><strong>「inplace 的 docker / ssh / local 四种」</strong><br> 没有这种组合。一个 thread 只有一个 <code>backend</code>。子 agent 可以单独写 <code>[sub.&lt;name&gt;] backend</code>（或 <code>&quot;none&quot;</code>），那也不是「inplace 套 docker」。</p><p><strong><code>local</code> 和 <code>inplace</code></strong><br> 都在本机跑。<code>local</code> = 草稿纸。<code>inplace</code> = 仓库本身。</p><p><strong><code>docker</code> 和 <code>inplace</code></strong><br> Docker 隔离的是 <em>shell</em>，不是你的项目。文件落在 <code>workspaces/main/</code>，除非你再拷。</p><p><strong><code>local</code> / <code>docker</code> / <code>ssh</code> 上的 <code>[project.local] path</code></strong><br> 那是 host_root（观察 / 拷贝），不是命令的 cwd。只有 <code>inplace</code> 把它当成 workdir。</p><p><strong><code>container</code> vs <code>docker</code> vs <code>podman</code></strong><br> 同一类后端。<code>container</code> 自动探测。两套 CLI 都在时，想钉死就写 <code>docker</code> 或 <code>podman</code>。</p><p><strong><code>inplace</code> 没选项目</strong><br> 没有目录会直接报错（必须有 <code>project_path</code>）。</p><p><strong>SSH 连不上就停不下来</strong><br> 以前 <code>connect</code> 没有超时，会堵住整条 wire。现在连接会超时；状态轮询也不会去唤醒 沙箱。仍连不上时查 Host 别名、密钥和网络。</p><p><strong><code>command_policy = &quot;workdir&quot;</code></strong><br> 四种后端都生效：<code>run_command</code> 不能带沙箱 workdir 以外的路径。<code>open</code> 关掉这项检查。</p><h2 id="相关" tabindex="-1">相关 <a class="header-anchor" href="#相关" aria-label="Permalink to &quot;相关&quot;">​</a></h2><ul><li><a href="./sandbox.html">Sandbox API</a> — <code>Runner.create</code>、工具、限额、错误</li><li><a href="./tools.html">工具</a> — 八个沙箱工具</li><li><a href="/pagent/zh/desktop.html">桌面端</a> — 本机 / 直接编辑 / 容器 / 远程</li><li><a href="/pagent/zh/vscode.html">VS Code</a> — <code>pagent.toml</code> 的 <code>[sandbox]</code></li><li>全字段模板：<code>src/template/pagent.toml</code></li></ul>`,62))])}const C=o(r,[["render",k]]);export{m as __pageData,C as default};
