## Cloud

这个目录专门放云端版本代码。

目标：

- 有用户系统
- 前后端分离
- 与当前本地版 `editors/*`、`src/app/*` 解耦

当前选型：

- 后端：FastAPI
- 前端：React
- 前端实现尽量和 `editors/web` 保持一致

当前只建立骨架，不迁移现有代码。

目录约定：

- `frontend/`：云端前端
- `backend/`：云端后端
